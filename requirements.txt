# Implements SRS general setup (Part 1 dependencies)
# Using >= constraints so the project works with Anaconda's pre-installed versions.

Flask==2.3.3
pandas>=2.0.3
numpy>=1.24.3
scikit-learn>=1.3.0
# Note: werkzeug is installed automatically with Flask — no need to list it separately.
